Always run `pnpm build` and confirm it passes before pushing code.
When asked how to finish a development branch, this user prefers option `2`:
push the branch and create a Pull Request. Do not make it draft unless explicitly told to do so.

If hosted database errors appear on Vercel, check whether the Supabase project is paused before debugging app code.
