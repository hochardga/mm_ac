"use client";

export function SignOutButton() {
  return (
    <button
      type="button"
      className="inline-flex rounded-full border border-stone-300 bg-transparent px-4 py-2 text-xs font-semibold uppercase tracking-[0.2em] text-stone-700 transition hover:border-stone-400 hover:text-stone-950"
    >
      Sign Out
    </button>
  );
}
